info:
  name: "Error: credenciales incorrectas en login"
  type: http
  seq: 12

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  body:
    type: json
    data: |-
      {
        "email": "admin@libreria.com",
        "password": "PasswordIncorrecta!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
